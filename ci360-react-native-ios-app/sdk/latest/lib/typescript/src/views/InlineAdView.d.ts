import { ViewStyle } from "react-native";
type Props = {
    spotId: string;
    style: ViewStyle;
};
declare const InlineAdView: import("react-native").HostComponent<Props> | (() => never);
export default InlineAdView;
